# ========================================
# School Announcements - Environment Variables
# ========================================
#
# Copy this file to .env and customize the values
# cp .env.example .env
#
# IMPORTANT: Never commit your .env file to version control!
# ========================================

# ----------------------------------------
# SECURITY SETTINGS (REQUIRED)
# ----------------------------------------

# API Authentication Key
# This is the password for the admin panel and API access
# Generate a secure key with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
API_KEY=change-this-in-production

# ----------------------------------------
# GENERAL SETTINGS
# ----------------------------------------

# Timezone (affects displayed times)
# See: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TZ=America/New_York

# CORS origin (for API access)
# Use * for local network, or specify allowed origins for production
CORS_ORIGIN=*

# ----------------------------------------
# STREAMING SETTINGS (OPTIONAL)
# ----------------------------------------

# Stream publish token - required for OBS to stream to MediaMTX
# This prevents unauthorized users from streaming to your displays
# In OBS: Use as Bearer Token (WHIP) or in Stream Key (RTMP)
# STREAM_PUBLISH_TOKEN=change-me-in-production

# ----------------------------------------
# WEATHER SETTINGS (OPTIONAL)
# ----------------------------------------

# OpenWeatherMap API Key
# Get a free key at: https://openweathermap.org/api
# Leave empty to disable weather widget
# WEATHER_API_KEY=your_api_key_here

# ----------------------------------------
# ADVANCED SETTINGS
# ----------------------------------------

# API Server Port (internal, usually don't change)
# PORT=3000

# Session expiry time in hours (default: 24)
# SESSION_EXPIRY_HOURS=24

# Maximum file upload size in MB (default: 10)
# MAX_UPLOAD_SIZE_MB=10

# ----------------------------------------
# PRODUCTION SETTINGS
# ----------------------------------------

# For internet-facing deployments, consider:
# - Using a reverse proxy (nginx, Traefik) with HTTPS
# - Setting CORS_ORIGIN to your specific domain
# - Using strong, randomly generated API_KEY
# - Enabling additional rate limiting

# Rate limit requests per minute (default: 100)
# RATE_LIMIT_PER_MINUTE=100
